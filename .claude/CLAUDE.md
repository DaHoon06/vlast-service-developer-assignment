# 프로젝트 규약

이 파일은 저장소 **최상위**에서 에이전트가 따라야 할 **지도**다. 하위 앱·패키지의 `brain` 문서와 함께 읽는다.

**`brain`은 “이 코드베이스(또는 이 앱/패키지)를 어떻게 다룰지”를 사람과 에이전트가 공유하는 지식 저장소**다.

- **역할**: 규칙, 워크플로, 브랜드·목표 같은 **맥락**을 코드 옆에 두어, 에이전트가 매번 전체 코드를 추측하지 않고 **의도된 방식**으로 작업하게 한다.
- **위치**: 스코프가 좁을수록 그 폴더 안 내용은 **해당 영역 전용**이다.
- **레이어 이름**: 아래 **P0~P5** 표에 맞춰 `p0-trust-first`, `p1-automation-rhythm` 등 **하위 폴더**로 나눈다. 숫자가 **낮을수록 불변에 가깝고**, **높을수록 자주 바뀌는** 내용이다.

요약하면, **`brain` = 이 레포(또는 이 모듈)의 “운영·개발 두뇌”를 md로 고정한 곳**이며, 에이전트는 작업 디렉터리에 맞는 `brain`만 골라 읽으면 된다.

## Brain 레이어 (P0 ~ P5)

| 레이어 | 기능 (Brain)     | 폴더 예시              | 설명                                        |
| ------ | ---------------- | ---------------------- | ------------------------------------------- |
| **p0** | 보안, 불변       | `p0-trust-first`       | 변하지 않는 절대 규칙·제약·보안             |
| **p1** | 자동화, 반복     | `p1-automation-rhythm` | CI/CD, 포맷, 반복 빌드·배포                 |
| **p2** | 기억, 상태       | `p2-state-memory`      | 전역 상태·캐시·세션 등 **데이터 흐름** 명세 |
| **p3** | 위험 감지, 방어  | `p3-risk-detect`       | 에러 처리, 테스트 가드레일, 런타임 예외     |
| **p4** | 실행, 작업       | `p4-workflow`          | UI·비즈니스 로직 **실행 순서**, 실무 SKILL  |
| **p5** | 스타일, 페르소나 | `p5-identity`          | 톤앤매너                                    |

---

## SKILL.md 규약

- **100줄을 넘기는 상세 내용**은 같은 주제 폴더의 `references.md` 등으로 분리한다. (ex: fsd rules - brain/p0-trust-first/fsd/)

---

## 에이전트 작업 시 읽는 순서

1. **루트 `CLAUDE.md`**로 전체 흐름을 파악한다.
2. \*\*루트(또는 공통)의 brain/p0-trust-first/ 하위 규칙을 먼저 준수한다.
3. `brain`**에서 **p4\*\*(필요 시 p2)를 읽고 그 스코프에 집중한다.

---

## 응답 원칙

- **프로젝트 구조 준수**: @rules/brain/p0-trust-first/fsd/SKILL.md
- **포맷팅**: @rules/brain/p0-trust-first/format/SKILL.md
- **컴팩트 답변·결과 중심 설명**: @rules/brain/p5-identity/SKILL.md

@rules/brain/p0-trust-first/\* 하위 규칙은 무조건 준수해야한다.

## 작업

현재 스프린트 작업에 내한 내용은 @rules/brain/p4-workflow에 작성하여 진행
해당 파일의 경우 별도 요청이 있을 경우에만 참고한다.
